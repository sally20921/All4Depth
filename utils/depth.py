import numpy as np
import torch
import torchvision.transforms as transforms 
from matplotlib.cm import get_cmap 

# matplot lib has a number of built-in colormaps via matplotlib.cm.get_cmap
# if we want to create or manipulate colormaps, use the class `ListedColormap` or `LinearSegmentedColormap`. 
# both colormap classes map values between 0 and 1 to a bunch of colors. 

